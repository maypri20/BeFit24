select * from user;

select min(weight_kg) from user; #39
select max(weight_kg) from user; #130

select min(height_cm) from user; #149

select max(height_cm) from user; #201

Workout Efficiency
AVG(calories_burned / session_duration_hours)
Calories burned per hour
Heart Rate Range
AVG(max_bpm - resting_bpm)
Intensity range
Intensity %
AVG((avg_bpm / max_bpm) * 100)
Relative workout effort

